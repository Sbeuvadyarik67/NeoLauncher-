"""
NeoLauncher — единый источник версии.

Правило: версия меняется ТОЛЬКО здесь.
README, CHANGELOG и title окна синхронизируются отсюда вручную (или автоматически).
"""

__version__ = "2.2.0"
__version_info__ = tuple(int(x) for x in __version__.split("."))